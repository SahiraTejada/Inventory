@model Inventory.Models.Brand

@{
    ViewData["Title"] = "Details";
}

<h1 class="mt-5">Details</h1>

<div>
    <h4>Brand</h4>
    <hr class="mb-4" />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.BrandName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.BrandName)
        </dd>
    </dl>
</div>
<div class="d-flex w-100 mt-5  justify-content-between">
    <a class="btn btn-success" role="button" asp-action="Edit" asp-route-id="@Model?.BrandId">Edit</a>
    <a class="btn btn-secondary d-flex align-items-center justify-content-center gap-1" role="button" asp-action="Index">
        <i class='bx bxs-chevrons-left icon-action'></i>
        Back to List
    </a>
</div>
